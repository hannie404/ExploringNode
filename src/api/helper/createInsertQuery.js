// exports.createInsertQuery = (...columns) => {
//   const columnsString = columns.join(",")
//   const columnsBinding = Array("?")
//   // `firstName`, `lastName`, `email`, `password`, `createdBy`, `updatedBy`
//   return `INSERT INTO `accounts` (${columnsString}) VALUES ( ?, ?, ?, ? ,?, ?)`
// }
